<!-- <div id="app">
  {{ message }}
</div> -->
<!-- <script type="text/javascript" src="residents.js">
  residentsPayload.forEach(function(element) {
    console.log(element);
  });
</script> -->

  <% if !current_user %>
    <div style="float: right; padding-left: 20px; padding-top: 10px;">
        <a class="btn btn-default" href="/login">Login</a>
    </div>
  <% else %>
    <a href="/logout">Log Out</a>
  <% end %>
  <!-- if current_user.role.nil? ? "You need to be an Admin" : <a href="/signup">Sign Up</a> -->
  <% if current_user.role == "admin" %>
    <a href="/signup">Sign Up</a>
  <% end %>

  <% if current_user.present? && current_user.role == "admin" %>
    <div style="float: right; padding-top: 10px;">
      <a class="btn btn-default" href="/signup">Sign Up</a>
    </div>
  <% end %>

<h1>All Residents!</h1> 
<table id="table1">

</table>

<tbody>
  <tr>
    <td>text</td>
  </tr>
</tbody>
<!-- <table>
  <tr>
    <td>
      
    </td>
  </tr>
</table> -->

<div style="float: left; padding-right: 50px;"><a href="/beds" class="btn btn-default">Beds</a></div>
<div><a href="/residents/new" class="btn btn-default">New Resident</a></div>
<br>
<div class="row">
  <% @residents.each do |resident| %>
    <div class="col-md-3">
      <div class="well">
        <div><h2>Name: <%= resident.first_name %>   <%= resident.last_name %></h2></div>
        <h3>Bed: Bed Name</h3>
        <h5>Gender: <%= resident.gender %></h5>
        <h5>Ethnicity: <%= resident.ethnicity %></h5>
        <h5>Date: <%= resident.date.strftime("%b %d, %Y") %></h5>
        <h5>HMIS Number: <%= resident.hmis_number %></h5>
        <h5>HMIS Entry Date: <%= resident.hmis_entry_date.strftime("%b %d, %Y") %></h5>
        <h5>Documented: <%= resident.documented.to_s.capitalize %></h5>

        <div>
        <a class="btn btn-default" href="/residents/<%= resident.id %>"> View Resident </a>
        </div>
        <br>
        <div class="btn btn-default">
        <%= link_to  'Delete', "/residents/#{resident.id.to_s}", method: :delete %>
        </div>
      </div>
    </div> 

    <button type="button" class="btm btn-success">Success</button>
    
  <% end %>